import 'package:flutter/cupertino.dart';

class WireMagazines extends StatelessWidget {
  const WireMagazines({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("The wire"),);
  }
}

